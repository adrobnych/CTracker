$('#mass_form').find('.check:checked').closest('tr').find('.status').text("<%= @new_state %>");
$(".notice").text("Important notice: Some countries have several currencies. All such currenices were marked as collected if you checked in at least one of them");

g_draw.line();
g_draw.pie();